{
    "name": "@next-game/realtime-worker",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=20.18.1"
    },
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:migrate:local": "wrangler d1 migrations apply IDENTITY_DB --local",
        "typegen": "wrangler types",
        "check": "tsc -p tsconfig.json --noEmit"
    },
    "devDependencies": {
        "typescript": "^6.0.2",
        "wrangler": "^4.78.0"
    }
}
